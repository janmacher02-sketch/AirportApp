node_modules/
.env
.env.*
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
Thumbs.db

# Keep seed JSON files in /data committed for the MVP.
# Production writes can be redirected to DATA_DIR.

# Original static design exports kept locally as reference, not needed for deploy.
airportready/
airport_detail_prg/
desktop_dashboard/
home_trip_planner/
premium_trip_pass/
report_wait_time/
trip_result/
